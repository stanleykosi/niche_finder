Read `AGENTS.md` and the canonical files under `docs/canonical/`, then build the full MVP from start to finish exactly to spec. This workstation is editing/control-plane only: do not install project dependencies, start services, build, launch browsers, run tests, or execute research locally. Deploy completed changes to Vercel/Railway and perform all fixture, integration, browser, migration, smoke, and live validation there. A local closed-test pass is not a deployment prerequisite; hosted failures are valid test evidence to diagnose, repair, redeploy, and verify.
